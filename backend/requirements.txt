# LaunchFolio — Python dependencies
# Install with: pip3 install -r requirements.txt

# Core — required for the app to run
pdfplumber>=0.10.0        # PDF text extraction
flask>=3.0.0              # Web server
flask-cors>=4.0.0         # Cross-origin support

# AI parsing — optional, install the one matching your provider
# Leave uninstalled to use the built-in rule-based parser instead.
#
# Anthropic (Claude):
#   pip3 install anthropic
#
# OpenAI (GPT-4o):
#   pip3 install openai
